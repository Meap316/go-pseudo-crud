package main

import "github.com/gin-gonic/gin"

func routes(r *gin.Engine) {

	// User Routes

	r.GET("/user/:id", )

	r.POST("/user", )

	r.PUT("/user/:id", )

	r.DELETE("/user/:id", func)

	r.POST("/login", func)
}
